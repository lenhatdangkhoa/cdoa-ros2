from setuptools import setup

package_name = 'pf_localization'

setup(
    name=package_name,
    version='0.1.0',
    packages=[package_name],
    data_files=[
        ('share/ament_index/resource_index/packages',
         ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
        ('share/' + package_name + '/launch', ['launch/pf_localization_single_anchor.launch.py']),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='Your Name',
    maintainer_email='you@example.com',
    description='Minimal-change ROS 2 port of PFLocalization.py for a single fixed RSSI node.',
    license='MIT',
    entry_points={
        'console_scripts': [
            'pf_localization = pf_localization.pf_localization:main',
        ],
    },
)
